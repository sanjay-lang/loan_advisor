services:
  - type: web
    name: loan-advisor
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: RESEND_API_KEY
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: GOOGLE_SHEETS_WEBHOOK_URL
        sync: false
